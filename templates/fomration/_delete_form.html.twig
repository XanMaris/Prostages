<form method="post" action="{{ path('fomration_delete', {'id': fomration.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ fomration.id) }}">
    <button class="btn">Delete</button>
</form>
